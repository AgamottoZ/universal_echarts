name: universal_echarts
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_echarts: ^2.0.0
  flutter_markdown: ^0.6.1
  html2md: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter